Place bundled module samples in this folder (`wavs/`).

Example:

- `wavs/kick.wav`
- `wavs/pad.wav`
- `wavs/texture.wav`

The plugin scans this folder at startup, sorts filenames alphabetically,
and uses `sample_index` to choose the active file.
